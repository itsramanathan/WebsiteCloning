# Website demo builder implementation handoff

User authorization: “Use humanlayer, genesis etc. to create it. I want it to be created autonomously. Use Sol / Terra for code generation. Use Fable to review. Astra can take the final call.” Prior instruction is keep it simple: URL -> Demo - Store Name, limited pages and catalog. Coordinator owns review and ledger; Sol owns implementation. No child agents or other model CLI calls. You are not alone: preserve every other workspace edit.

## Ownership
Only app/**. This is an isolated implementation checkout with its own Genesis ledger. Do not edit research, root manifests, Genesis canonical files, Agentic Commerce/**, global configuration or other projects. Read docs/SIMPLIFIED_PLAN.md. This bounded milestone implements the internal URL-to-website preview generator and a documented widget integration seam. A candidate widget was inspected read-only in a separate project. Do not modify that project. Its config uses storeId, apiKey, apiUrl; chat posts session_id/message to /api/v1/widget/chat; cart posts session_id/product_id/quantity to /api/v1/widget/cart/add. onAddToCart runs AFTER the backend mutation and cannot safely intercept it. Therefore embedding without a demo-specific backend/catalog/session adapter is not authorized as a verified integration. Record this concrete seam in integration notes. Do not invent a new agent or claim existing agent integration works. Do not integrate the unrelated standalone commerce app.

## Operational adjustments from inspected environment
Node20 exists; Docker daemon, hosted target and model API credentials do not. Use a single Node application with a small dependency set and local durable storage. No per-demo deployments. Implement public HTML extraction with connection-pinned, public-address-only HTTP(S) requests; do not execute source JavaScript. This deliberately supports server-rendered sites first, with explicit errors for JavaScript-only/blocked pages. Do not silently implement unsafe Playwright capture. Do not add unavailable browser/model paths as if working. Model-driven visual analysis, isolated browser capture, actual widget integration and remote hosting remain visibly unverified dependencies. Provide a concrete deployment recipe for this same app with HTTPS, persistent volume and operator secret; do not purchase or deploy anything remotely.

## Deliverable
A polished compact internal dashboard (URL form -> building progress -> preview -> Share/Retry/Delete), homepage/collection/item views, and session cart/quote. Name each output Demo - original name, show Demo - sample products and information. No real orders. Dashboard should clearly explain this is a website preview until widget integration exists. Use accessible native forms, readable warm-neutral surfaces, clear typography, understated accent, responsive content. No marketing landing page, fake metrics or fake testimonials. Preserve site-specific header/hero/image/color/grid character through trusted component variants and captured evidence; no executable source HTML/CSS injection. Document limitations honestly.

Maximum4pages (home, collection,2details),6products or3B2Bofferings,30images,25MBsavedassets,5minattempt. Extract JSON-LD + visible headings/cards/images/prices, unknown stays unknown; fail if no credible catalog. Same-origin page selection, validated CDN assets. Download only supported safe raster types with bounded reads; do not inline SVG/script or hotlink remote resources. Capture colors/spacing/layout hints conservatively from static inline/linked styles only through a bounded parser and safe value allowlists if needed. No arbitrary source JS, custom dependency generation, checkout/forms or hidden login. Store source URLs and extraction limitations.

Public network fetch must defend SSRF: http/https only, reject credentials/nonstandard ports, validate ALL resolved IPv4/IPv6 addresses against nonpublic ranges (including mapped/reserved/linklocal/metadata), pin connection to validated address while preserving hostname/TLS verification, validate each redirect, bounded compressed and decompressed responses, request timeout and total attempt deadline. Never perform a second uncontrolled DNS resolution or inherit proxy credentials. Test connection pinning and redirect rejection. No source script/XHR/frame execution means no browser egress loophole.

Management requires configured/random startup operator credential and secure cookie login + CSRF; default bind loopback. No auth secrets in responses or URLs. Sharing uses separate revocable >=128bit token scoped demo/assets; slug alone grants nothing. Share starts disabled; only ready current artifacts share; Retry disables sharing; Delete revokes visitor sessions/assets and prevents late publication. Each viewer separate cart/session. Set explicit quantity with revision; reject stale agent/human writes. Quote draft local, no real order/checkout/email. Expose small same-origin integration API/catalog/currentpage/navigation/session actions for existing widget. Describe its contract; do not show a fake chatbot or claim protocol interoperability.

Job state building/ready/failed, immutable attempt artifacts; restart/stale timeout recovery, current-attempt+deadline+deleted compare before publish, retry reuse record and fresh attempt folder, no incomplete ready artifacts. One job at a time suffices, reject/queue bounded requests. Bound login/generation rate and disk use. Make user errors concise/actionable.

## Verification and handoff
Create verify.sh with meaningful Node tests: static retail and B2B fixture extraction; URL/SSRF/pinning/redirect limits; management auth/CSRF; sharing/revoke/assets; separate visitor sessions/stale revisions; retry/delete/late worker/restart; missing facts; bounded output. Fixtures must be clearly test-only and never weaken production fetch validation. Perform live public-source smoke if feasible and record exact site/results, not universal resemblance. Browser QA desktop+phone for dashboard and three views using available tooling; screenshots under app/review. Do not install global packages or add paid accounts. Fable review will be requested by coordinator after stable source; do not self-review via another model.

README with exact start/test commands, localhost URL (prefer4320; avoid4310 used elsewhere), capabilities and missing integration/hosting. Save implementation evidence and explicit deviations under app/review. Lock dependencies. No Git commit or root file changes. Return changed files, executed checks/results, live source results, and limitations.

The user explicitly authorized parallel independent initiatives and independent Fable reviews. This checkout is isolated from the commerce initiative. Do not edit any other checkout. The coordinator will copy delivered files to the original Website cloning folder after reconciliation. Use this handoff as the bounded current scope; original plan is broader intended end state.
